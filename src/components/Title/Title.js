import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Clicky Game!</h1>
        <p>Try to click all the characters without repeating. The more times you click, the more points you earn.</p>
    </div>
);
export default Title;